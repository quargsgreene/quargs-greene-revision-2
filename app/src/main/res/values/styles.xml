<resources>

    <!-- Base application theme. -->
    <style name="Theme.Stacker" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Use the colors defined in colors.xml -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="android:windowBackground">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/purple_700</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Theme for activities without an action bar. -->
    <style name="Theme.Stacker.NoActionBar" parent="Theme.Stacker">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Other items to customize for this theme can go here -->
    </style>

    <style name="Base.Theme.Stacker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

</resources>